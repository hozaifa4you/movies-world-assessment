import { HeroSection } from '@/components/home/hero-section';

const HomePage = () => {
   return (
      <main className="w-full">
         <HeroSection />
      </main>
   );
};

export default HomePage;
